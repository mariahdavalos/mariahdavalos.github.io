{
    "name": "node-proxy",
    "version": "1.0.0",
    "proxy": "http://localhost:8082",
    "main": "index.js",
    "license": "MIT",
    "dependencies": {
        "@jamesives/github-pages-deploy-action": "^4.1.5"
    },
    "scripts": {
        "start": "npm start --prefix api & npm start --prefix app",
        "predeploy": "npm build --prefix app",
        "deploy": "gh-pages -d build"
    },
    "devDependencies": {
        "gh-pages": "^3.2.3"
    }
}